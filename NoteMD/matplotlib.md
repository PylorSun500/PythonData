## pyplot - 绘图接口

sentence: 
```python
from matplotlib import pyplot # as plt
```
alias: `plt`

### pyplot.plot() - 折线图
grammar:
`plot(x, y, fmt, scalex=[bool], scaley=[bool], data=, label=)`
proceed: 
```python
x = []
y = []
plt.plot(x, y)
plt.show()
```
